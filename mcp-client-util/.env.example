# ── MCP Server URL (Jira + Git are accessed via this server) ─────
# The MCP server handles its own Jira/Git credentials internally.
MCP_SERVER_URL=http://localhost:3000

# ── Jira project key (used in API calls to the MCP server) ────────
JIRA_PROJECT_KEY=US
